[project]
name = "scraply-ai-agent"
version = "1.0.0"
description = "AI Agent system for Scraply waste management platform"
requires-python = ">=3.10"

[project.scripts]
scraply-agent = "main:main"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
